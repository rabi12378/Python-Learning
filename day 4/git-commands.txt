1. Git is a Version COntrol system
2. It maintains the code history so that we can go to any version of the code at any required moment.
3.Git has major two significances:
    -To maintain code versions
    -To collaborate in the team works
Repository= It is nothing but a git project folder

- There are two Repository 
- One is Local repository (Repo)
-Another is remore repository(Repo)

#git commands
1. Git init
-initialize a repo in Local

2.git add .
- add all the changes to the git

3. git commit -m ""
- commit all the codes